<h1>All our Flats</h1>

<% @flats.each do |flat| %>
  <div>
    <h2><%= flat.name %></h2>
    <p><%= flat.address %></p>
    <p><%= flat.description %> </p>
    <p><%= flat.price_per_night %> <%= flat.number_of_guests %></p>
  </div>
<% end %>
